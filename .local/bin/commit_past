#!/bin/bash

branch_prompt="Select a branch (default: main): "
date_prompt="Select a date (e.g.: 2022-10-10 | 1 day ago | 2 weeks ago): "
read -rp "$branch_prompt" branch
read -rp "$date_prompt" date
branch="${branch:-main}"
git rev-list -n1 --before="$date" "$branch"
